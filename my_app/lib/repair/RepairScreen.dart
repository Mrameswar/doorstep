import 'package:flutter/material.dart';

class RepairScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Repair Screen'),
      ),
      body: Center(
        child: Text('Welcome to the Repair Screen!'),
      ),
    );
  }
}
