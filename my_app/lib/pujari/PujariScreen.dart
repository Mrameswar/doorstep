import 'package:flutter/material.dart';

class PujariScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pujari Screen'),
      ),
      body: Center(
        child: Text('Welcome to Pujari Screen!'),
      ),
    );
  }
}
