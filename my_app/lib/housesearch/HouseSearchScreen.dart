import 'package:flutter/material.dart';

class HouseSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('House Search'),
      ),
      body: Center(
        child: Text('Welcome to House Search Screen!'),
      ),
    );
  }
}
