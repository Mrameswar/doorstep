import 'package:flutter/material.dart';

class CleaningScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cleaning Screen'),
      ),
      body: Center(
        child: Text('Welcome to the Cleaning Screen!'),
      ),
    );
  }
}
